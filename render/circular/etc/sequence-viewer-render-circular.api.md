## API Report File for "@anocca/sequence-viewer-render-circular"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { CircularSelection } from '@anocca/sequence-viewer-utils';
import { DrawFunction } from '@anocca/sequence-viewer-utils';
import { RenderData } from '@anocca/sequence-viewer-utils';
import { SelectionRange } from '@anocca/sequence-viewer-utils';

// @internal
export const bindValue: (value: {
    zoom: number;
    angle: number;
    radius: number;
}, target: {
    zoom: number;
    angle: number;
    radius: number;
}) => void;

// @public
export const drawCircular: DrawFunction;

// @internal
export const getCircleProperties: ({ w, h, data, sequence, circularSelection }: {
    sequence: string;
    w: number;
    h: number;
    data: RenderData;
    circularSelection: CircularSelection[];
}) => {
    circleY: number;
    angleDelta: number;
    angleOffset: number;
    radius: number;
    len: number;
    iLen: number;
    mouseAngle: number;
    hoveringCaretPosition: number;
    mouseRadius: number;
};

// @internal
export const getRadiusTargetForViewRange: (w: number, h: number, len: number, zoom: number, viewRange: SelectionRange) => number;

// @internal
export const increase: (zoom: number, value: {
    zoom: number;
    angle: number;
    radius: number;
}, target: {
    zoom: number;
    angle: number;
    radius: number;
}) => void;

```
