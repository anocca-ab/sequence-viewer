## API Report File for "@anocca/sequence-viewer-render-linear"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { DrawFunction } from '@anocca/sequence-viewer-utils';
import { Matrix } from '@anocca/sequence-viewer-utils';

// @internal
export const baseHeight = 20;

// @public
export const drawLinear: DrawFunction;

// @internal
export const getBaseColor: (isProtein: boolean, ntOrAa: string) => string;

// @internal
export const getBaseName: (level: 0 | 1 | 2 | 3, isProtein: boolean, ntOrAa: string) => string;

// @internal
export const getInverseMatrix: (m: Matrix) => Matrix;

// @internal
export const getSelectedSequence: (currentMousePosition: {
    x: number;
    y: number;
}, initialMousePosition: {
    x: number;
    y: number;
}, matrix: Matrix, sequence: string, isProtein: boolean) => {
    width: number;
    x: number;
    sequence: string;
    sequenceStart: number;
    sequenceEnd: number;
};

// @internal
export const scaleRectByMatrix: (x: number, y: number, width: number, height: number, matrix: Matrix) => {
    x: number;
    y: number;
    width: number;
    height: number;
};

// @internal
export const transformPoint: (x: number, y: number, m: Matrix) => {
    x: number;
    y: number;
};

// @internal
export const yBase: number;

```
