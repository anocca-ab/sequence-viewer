## API Report File for "@anocca/sequence-viewer-utils"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @internal
export const aas: {
    [k: string]: {
        value: string;
        name: string;
        threeLettersName: string;
        hydrophobicity?: number;
        color: string;
        doInvert?: boolean;
        isAmbiguous?: boolean;
        aliases?: string;
    };
};

// @public
export type Annotation = {
    id: string;
    label: string;
    color: string;
    locations: [number, number][];
    hidden: boolean;
    displayAsSequence: boolean;
    rightTag: string;
    displayLabel: string;
    direction: SeqAnnotationDirectionsEnum;
} & ({
    type: 'DNA_OLIGO';
    fivePExtension: string;
} | {
    type: 'DNA_RE_NUC';
    cleavageSites: [number, number][];
} | {
    type: 'OTHER';
});

// @public
export type AnnotationFormProps = {
    onSubmit: (annotation: Annotation) => void;
    title: string;
    initialValues: {
        [k in keyof Annotation]?: Annotation[k];
    };
};

// @internal
export const annotationIsOverOrigin: (loc: [number, number], direction: SeqAnnotationDirectionsEnum) => boolean;

// @public
export type Annotations = Annotation[];

// @internal
export const anoccaPalette: {
    primaryText: string;
    primaryBackground: string;
    websiteSecondary: string;
    websiteSecondaryLight: string;
    websiteSecondaryDark: string;
    websiteTertiary: string;
    websiteTertiaryDark: string;
    websiteTertiaryLight: string;
    secondaryText: string;
    secondaryBackground: string;
    accent1: string;
    accent1Light: string;
    accent1Dark: string;
    accent2: string;
    accent3: string;
    accent3Light: string;
    accent3Dark: string;
    accent4: string;
    accent4Light: string;
    accent4Dark: string;
    accent5: string;
    accent5Light: string;
    accent5Dark: string;
    accent6: string;
    accent6Light: string;
    accent6Dark: string;
    link: string;
};

// @public
export type ChromatogramData = {
    aTrace: number[];
    gTrace: number[];
    cTrace: number[];
    tTrace: number[];
    phred: number[];
    peakLocations: number[];
    sequence: string;
};

// @public
export type CircularCameraProgress = {
    zoom: number;
    angle: number;
    radius: number;
};

// @public
export type CircularSelection = (SelectionRange & {
    state: 'selecting';
    antiClockwise?: boolean;
}) | (SelectionRange & {
    state: 'selected';
    antiClockwise?: boolean;
});

// @internal
export const complementBases: {
    [k: string]: string;
};

// @internal
export const createScaleMatrix: (scale: number) => Matrix;

// @internal
export const createTranslateMatrix: (x: number, y: number) => Matrix;

// @internal
export const debounce: <T extends (...args: any[]) => any>(fn: T) => T;

// @internal
export const dnaBaseWidth = 24;

// @internal
export const dnaColors: {
    [k: string]: string;
};

// @public
export type DrawFunction = (props: {
    c: CanvasRenderingContext2D;
    w: number;
    h: number;
    ratio: number;
    data: RenderData;
    sequence: string;
    circularSelection: CircularSelection[];
    searchResults: {
        start: number;
        end: number;
        active: boolean;
        complement: boolean;
    }[];
    filterChromOptions: string[];
    annotationLevels: Annotations[];
    renderStateRef: {
        clickedFeatures: string[];
        hoveringFeature: undefined | string;
    };
    codons: {
        [k: string]: string;
    };
    isProtein: boolean;
    chromatogramData?: ChromatogramData;
}) => {
    clickedFeatures: string[];
    hoveringFeature: undefined | string;
};

// @internal
export const getAaColor: (aa: string) => string;

// @public
export function getFont(size: number, weight?: 'normal' | 'bold' | number): string;

// @internal
export const getIndexMid: (start: number, end: number, len: number, antiClockwise?: boolean | undefined) => number;

// @internal
export const getNtColor: (nt: string) => string;

// @internal
export const getNtComplement: (n: string) => string;

// @public
export function getRatio(context: CanvasRenderingContext2D): {
    ratio: number;
    shouldScale: boolean;
};

// @internal
export const getReverseComplement: (isProtein: boolean, nucleotides: string) => string;

// @internal
export const getSelectionDelta: (len: number, antiClockwise: boolean, start: number, end: number) => number;

// @internal
export const getSelectionDeltaAngle: (len: number, antiClockwise: boolean, start: number, end: number) => number;

// @internal
export const getSelectionLabel: (circularSelection: CircularSelection, isProtein: boolean, sequenceSize: number) => string;

// @internal
export const getSelectionOver: (i: number, circularSelection: CircularSelection[]) => CircularSelection | undefined;

// @internal
export const getTextColor: (background: string) => "black" | "white";

// @internal
export const humanCodons: {
    TAG: string;
    TAA: string;
    TGA: string;
    GCG: string;
    GCA: string;
    GCT: string;
    GCC: string;
    TGT: string;
    TGC: string;
    GAT: string;
    GAC: string;
    GAA: string;
    GAG: string;
    TTT: string;
    TTC: string;
    GGT: string;
    GGA: string;
    GGG: string;
    GGC: string;
    CAT: string;
    CAC: string;
    ATA: string;
    ATT: string;
    ATC: string;
    AAA: string;
    AAG: string;
    CTA: string;
    TTA: string;
    CTT: string;
    TTG: string;
    CTC: string;
    CTG: string;
    ATG: string;
    AAT: string;
    AAC: string;
    CCG: string;
    CCA: string;
    CCT: string;
    CCC: string;
    CAA: string;
    CAG: string;
    CGT: string;
    CGA: string;
    CGC: string;
    AGA: string;
    AGG: string;
    CGG: string;
    TCG: string;
    AGT: string;
    TCA: string;
    TCT: string;
    TCC: string;
    AGC: string;
    ACG: string;
    ACT: string;
    ACA: string;
    ACC: string;
    GTA: string;
    GTT: string;
    GTC: string;
    GTG: string;
    TGG: string;
    TAT: string;
    TAC: string;
};

// @internal
export const isInSelection: (i: number, circularSelection: Omit<CircularSelection, 'state'>) => boolean;

// @internal
export const isRangeInSelection: ([a, b]: [number, number], circularSelection: Omit<CircularSelection, 'state'>) => boolean;

// @internal
export const isRangeOverOrigin: (selectionRange: SelectionRange, antiClockwise: boolean) => boolean;

// @internal
export const isSelectionOverOrigin: (circularSelection: CircularSelection) => boolean;

// @public
export type Matrix = {
    a: number;
    b: number;
    c: number;
    d: number;
    e: number;
    f: number;
};

// @internal
export const matrixMultiply: (...matrices: Matrix[]) => {
    a: number;
    b: number;
    c: number;
    d: number;
    e: number;
    f: number;
};

// @internal
export const maybeRenderOligo: (feature: Annotation, location: [number, number], len: number, onRenderBase: (i: number, base: string) => void) => void;

// @internal
export const packAnnotations: (annotations: Annotations, sequenceLength: number) => Annotations[];

// @internal
export const proteinBaseWidth = 150;

// @public
export type RenderData = {
    mouseX: number;
    mouseY: number;
    circularCamera: {
        angleOffset: number;
        scrollOffsetZoomed: number;
        scrollOffsetZooming: number;
        value: CircularCameraProgress;
        target: CircularCameraProgress;
    };
    matrix: Matrix;
};

// @internal
export const renderSequenceInSelectionRange: (props: {
    len: number;
    startIndex: number;
    endIndex: number;
} & ({
    sequence: string;
    onRenderBase: (i: number, base: string) => void;
} | {
    onRenderBase: (i: number) => void;
})) => void;

// @public
export function scaleBuffer(canvas: HTMLCanvasElement, width: number, height: number): {
    ratio: number;
    shouldScale: boolean;
} | undefined;

// @public
export type SearchResult = {
    start: number;
    end: number;
    complement: boolean;
    active: boolean;
};

// @public
export type SelectionRange = {
    start: number;
    end: number;
};

// @public
export enum SeqAnnotationDirectionsEnum {
    // (undocumented)
    FORWARD = "FORWARD",
    // (undocumented)
    NOT_DEFINED = "NOT_DEFINED",
    // (undocumented)
    REVERSE = "REVERSE"
}

// @public
export type SequenceControllerRef = {
    onClickAnnotation: (id: string | undefined, append?: boolean | undefined) => void;
};

// @internal
export const sequenceMatch: (isProtein: boolean, sequence: string, site: string) => boolean;

// @internal
export function shouldInvertColor(a: string): boolean;

// @internal
export function tuple<A, B, C, D>(a: A, b: B, c: C, d: D): [A, B, C, D];

// @internal
export function tuple<A, B, C>(a: A, b: B, c: C): [A, B, C];

// @internal
export function tuple<A, B>(a: A, b: B): [A, B];

```
